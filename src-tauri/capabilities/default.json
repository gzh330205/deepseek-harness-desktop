{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the DSH Desktop launcher",
  "windows": ["main", "settings", "about", "update-overlay", "desktop-update", "dsh-update-prompt"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "updater:default"
  ]
}
