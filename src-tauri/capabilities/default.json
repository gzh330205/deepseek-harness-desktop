{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the DSH Desktop launcher",
  "windows": ["main", "settings", "about", "update-overlay", "desktop-update"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
